import math

print(round(2.6)) #rounds the number to the nearest integer
print(abs(-2.5)) #returns the absolute value of the number

print(math.ceil(2.5)) #rounds the number up to the nearest integer
print(math.floor(2.9)) #rounds the number down to the nearest integer
print(math.sqrt(16)) #returns the square root of the number